export default class ContentTemplate {
    constructor(user, aiModel, title, body) {
        this.user = user;
        this.aiModel = aiModel;
        this.title = title;
        this.body = body;
    }
}
